# 1. CRITICAL SPEEDUP: Forces pip to download the ~200MB CPU version of PyTorch instead of the ~2.5GB GPU version
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Core Application ---
streamlit>=1.26.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
httpx>=0.24.0
pydantic>=2.0
tenacity>=8.2.2
python-dotenv>=1.0.0
python-json-logger>=2.0.7
colorama>=0.4.6

# --- LangChain Ecosystem ---
langchain>=0.1.34
langchain-core>=0.1.34
langchain-community>=0.0.31
langchain-anthropic>=0.1.10
langchain-openai>=0.3.0

# --- Vector Databases ---
chromadb>=0.4.15
pinecone-client>=3.1.0       # Uncomment if you actively switch to Pinecone in config.py
langchain-pinecone>=0.1.0    # Uncomment if you actively switch to Pinecone in config.py

# --- Retrieval & Re-ranking (Week 3) ---
sentence-transformers>=2.6.1
# Note: 'torch' and 'transformers' are automatically installed by sentence-transformers

# --- Document Parsing & Ingestion (Week 3) ---
# Replaced massive unstructured OCR dependencies with lightweight native parsers
pypdf>=4.0.0
unstructured>=0.12.5
python-docx>=1.1.0
markdown>=3.5.2

# --- Evaluation Harness (Week 3) ---
ragas>=0.1.4
datasets>=2.18.0

langchain-cohere 
langchain-experimental